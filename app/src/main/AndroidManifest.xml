<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.got.bestapps.gameofthrones">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".game.GameActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".game.GameOverActivity" />
        <activity android:name=".stats.StatsActivity" />
        <activity android:name=".rules.RulesActivity" />
        <activity android:name=".game.CountdownActivity" />

        <service
            android:name="com.got.bestapps.gameofthrones.services.IncrementLifesService"
            android:enabled="true" >
        </service>

        <receiver
            android:name="com.got.bestapps.gameofthrones.services.IncrementLifesBroadcastReciver"
            android:enabled="true"
            android:exported="true"
            android:label="RestartServiceWhenStopped">
            <intent-filter>
                <action android:name="com.got.bestapps.gameofthrones.services.IncrementLifesService"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>